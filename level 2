String str = input;
Int length = str.length();
if(l%2==0)
{
char ch1=str.charAt((l/2)-1);
}
else
{
char ch = str.charAt(l/2);
}
for (int i = 0 ; i <= mid ; i++)
{   
System.out.print(s.charAt(i)); 
}
